[tool.poetry]
name = "gerador-nestjs-gpt"
version = "0.1.0"
description = "Gerador de código NestJS usando GPT"
authors = ["Seu Nome <email@exemplo.com>"]

[tool.poetry.dependencies]
python = "^3.12"
openai = "^1.0.0"
